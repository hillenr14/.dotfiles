# Robert's Bootstraping Scripts and dot files

## Installation:

On an Arch based distribution as user, run the following:

```
curl -LO https://github.com/hillenr14/.dotfiles/raw/master/install.sh
sh install.sh
```

That's it.
